.idea/
__pycache__/
*.pyc

# Runtime state written by the currently-active read path on every
# query/render - churns every session, unlike the frozen _mcp_diag*
# files (superseded plugins, kept tracked as historical evidence - see
# PLAN.md/ROADMAP.md).
_mcp_response.json
_mcp_ring_debug.log
_mcp_render_status.json
_mcp_render_debug.log

# A different team's LightWave-MCP project (targeting LightWave
# 2025.0.3+), downloaded locally for research/comparison purposes only
# - see PLAN.md "Bone chain traversal" for what was learned from it.
# Not part of this project, never meant to be committed here.
lightwave-mcp-master-2025/
